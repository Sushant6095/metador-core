/**
 * metador-crank — DCA ticks + expiry sweeps (G1) and the public `npx metador-crank`
 * CLI. Untrusted by design: the chain re-verifies every call (CLAUDE.md §10).
 * Stub until keel_core ships vault + strategy objects to tick.
 */
function main(): void {
  console.log('metador-crank: stub — no mandates to tick yet (keel_core lands in G1)');
}

main();
