{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Browser package (window guard) that reads NEXT_PUBLIC_* env at build.
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["src"]
}
