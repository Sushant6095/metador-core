{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // page.evaluate callbacks execute in the browser — they need DOM types.
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["src"]
}
