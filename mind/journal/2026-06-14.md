- 17:39:23 - Write - /Users/vyapar/Downloads/keel/apps/web/app/components/hero/HeroField.tsx
- 17:39:37 - Edit - /Users/vyapar/Downloads/keel/apps/web/app/components/hero/HeroComposition.tsx
- 17:39:56 - Edit - /Users/vyapar/Downloads/keel/apps/web/app/components/hero/HeroComposition.tsx
- 17:40:13 - Edit - /Users/vyapar/Downloads/keel/apps/web/app/components/hero/HeroComposition.tsx

## Task — Living hero field (founder parity directive: "same dynamic effects")
- **Goal:** match the Hyperliquid hero's signature dynamic effect (a living,
  morphing organic field) in Keel's brand — the one benchmark effect the
  landing still lacked (hero was a static graded slot; armature benched).
- **Decision:** Keel-brand parity, NOT a clone. §8 + the legal-safe line bar
  shipping their green palette / serif wordmark / logo / copy / Spline. Built
  the *caliber* equivalent: brass-on-slate, our logo, Fraunces, original copy.
- **Actions:** new `HeroField.tsx` — 4 pre-blurred brass radial blobs over the
  slate base, `mix-blend-mode:screen` so soft edges glow-MERGE like metaballs;
  only `transform`+`opacity` animate (compositor 60fps); slow desynced drift
  (22–34s); reduced-motion → static rest field. Wired into HeroComposition
  (kept instant base grade + radials for LCP; removed the two old glow divs).
- **Files:** HeroField.tsx (new), HeroComposition.tsx.
- **Verify:** typecheck+lint+test+prettier all green. Live DOM on :3005 = 4
  blobs, screen-blend, blur(44px). Shots web-v2/{390,1440}-hero.png: warm brass
  glow-merge right-of-center, monumental headline, no overflow at 390.
- **Surprise / flag:** pre-existing console errors found on the landing (NOT
  mine, in ActivityFeedSection): `<li>` nested inside `<li>` (ActivityRow
  renders an li inside motion.li) → hydration mismatch; and an `<svg height>`
  set to "auto". Both predate this task — flagged to founder, not drive-by
  fixed (§ "every changed line traces to the task").
- **Motion-budget note:** choreography.md §4 caps ambient movers at one; the
  founder parity directive explicitly overrides that for the hero. Scoped to
  the hero only — rest of the site keeps §4.
- 18:03:43 - Edit - /Users/vyapar/Downloads/keel/mind/journal/2026-06-14.md
- 18:04:09 - Edit - /Users/vyapar/Downloads/keel/WORKLOG.md
